.DS_Store
.gradle
.idea
.intellijPlatform
.kotlin
# Anchored: a bare `build` also matches source directories, and silently swallowed the
# `org/apache/solr/ide/build` package the first time buildSrc was committed.
/build/
/buildSrc/build/
/demo/build/
